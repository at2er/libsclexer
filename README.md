# Simple C Lexer
A simple and convenient library for lexer.

## Use
Just include these single-header file `sclexer.h`.
And define their `IMPL` macros to get the definitions for compiling.
- `#include "sclexer.h"`: `SCLEXER_IMPL`
